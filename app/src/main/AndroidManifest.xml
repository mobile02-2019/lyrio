<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lyrio">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/lyr_new_icon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/lyr_new_icon_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.NoActionBar">
        <activity android:name=".ListaDeArtistasActivity" />
        <activity android:name=".ListaAlbumActivity" />
        <activity android:name=".LoginActivity" />
        <activity android:name=".TabMenu" />
        <activity android:name=".UserEsqueciMinhaSenha" />
        <activity android:name=".UserCadastroActivity" />
        <activity android:name=".SplashActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>